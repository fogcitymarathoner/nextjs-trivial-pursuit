rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write only to authenticated users (default rule)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }

    // Test collection - allow public read for testing purposes
    match /test/{document=**} {
      allow read: if true;  // Allow anyone to read for testing
      allow write: if request.auth != null;  // Only authenticated users can write
    }

    // Specific rules for users collection
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    // Specific rules for products collection
    match /products/{productId} {
      allow read: if true;  // Anyone can read products
      allow write: if request.auth != null;  // Only authenticated users can write
    }
  }
}